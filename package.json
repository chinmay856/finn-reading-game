{
  "name": "finn-reading-game",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "check": "node --check app.js && node --check reading-engine.js && node --check speech/audio-capture.js && node --check speech/local-whisper-recognizer.js && node --check speech/whisper-worker.js && node --check vite.config.js",
    "dev": "vite",
    "preview": "vite preview",
    "test": "node --test"
  },
  "dependencies": {
    "@huggingface/transformers": "3.7.1"
  },
  "devDependencies": {
    "vite": "8.1.4"
  }
}
