import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";const files=Promise.all([readFile(new URL("../app.js",import.meta.url),"utf8"),readFile(new URL("../apps/internet-recovery/spottyfi.css",import.meta.url),"utf8"),readFile(new URL("../index.html",import.meta.url),"utf8")]);test("Spotty-Fi exposes silent tracks, queue, history drawer, evidence, and companion",async()=>{const[app,css,html]=await files;assert.match(html,/CANONICAL FICTIONAL SILENT LIBRARY/u);assert.match(html,/id="spottyfiTrackList"/u);assert.match(html,/Credits, history, and suggestions/u);assert.match(html,/CASE FILE · SLOT 9/u);assert.match(app,/openSpottyFiExperience/u);assert.match(css,/@media\(max-width:1279px\)/u)});test("Spotty-Fi ships no audio, video, lyrics, or percentage progress",async()=>{const[,,html]=await files;const s=html.slice(html.indexOf('<section id="spottyfi"'),html.indexOf('<section id="mapguess"'));assert.doesNotMatch(s,/<(?:audio|video|iframe)\b/iu);assert.doesNotMatch(s,/lyrics?\s*:/iu);assert.doesNotMatch(s,/\b\d+%/u)});
