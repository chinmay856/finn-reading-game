#!/usr/bin/env node

import crypto from "node:crypto";
import fs from "node:fs";
import path from "node:path";
import { INTERNET_RECOVERY_COLORS as COLORS } from "./lib/internet-recovery-design-system.mjs";

const outDir = path.resolve("docs/design/screens/2026-08-16/mycorner-production");
const outSvg = path.join(outDir, "mycorner-anchor-master-v1.svg");
const outReview = path.join(outDir, "mycorner-anchor-review-v1.html");
const shellPath = path.resolve("docs/design/screens/2026-08-15/wikiwhy-inkscape-spike/wikiwhy-phase-one-master-v2.svg");
const amyPath = path.resolve("apps/internet-recovery/art/characters/dialogue/amy-supportive.jpg");
fs.mkdirSync(outDir, { recursive: true });

const shell = fs.readFileSync(shellPath, "utf8");
const defs = shell.match(/<defs>([\s\S]*?)<\/defs>/)?.[1];
const styles = shell.match(/<style>([\s\S]*?)<\/style>/)?.[1];
if (!defs || !styles) throw new Error("Reviewed shared shell unavailable.");
const shellHash = crypto.createHash("sha256").update(defs).digest("hex");
const sharedDefs = defs.replaceAll('href="assets/', 'href="../../2026-08-15/wikiwhy-inkscape-spike/assets/');
const amyAsset = path.relative(outDir, amyPath);

const states = [
  { id: "initial", label: "Initial impersonation", progress: 0, phase: "phase-1", delta: 0, mode: "corrupt" },
  { id: "repaired", label: "Identity context restored", progress: 100, phase: "phase-1", delta: 3, mode: "repaired" },
  { id: "auto-overfix", label: "Auto identity upscale", progress: 0, phase: "phase-2", delta: 0, mode: "auto" },
  { id: "lock-repair", label: "Lock in identity checks", progress: 0, phase: "phase-2", delta: 0, mode: "auto", checklist: true },
];

const titlebar = () => `<g><rect x="112" y="24" width="520" height="29" fill="url(#titleGradient)"/><text x="126" y="46" class="window-title">www.my-corner.com</text><rect x="112" y="861" width="188" height="31" fill="url(#buttonGradient)" stroke="#6d6d67" stroke-width="1.3"/><text x="54" y="882" class="task-label" text-anchor="middle">START</text><text x="146" y="882" class="task-label">MYCORNER</text></g>`;

function header() {
  return `<g data-qa-box="109,56,911,150"><rect x="109" y="56" width="802" height="56" fill="#4A1F78"/><rect x="124" y="69" width="28" height="28" rx="5" fill="#EB4EA4"/><rect x="140" y="81" width="28" height="28" rx="5" fill="#45B7D1"/><rect x="157" y="72" width="28" height="28" rx="5" fill="#A4D64B"/><text x="201" y="96" class="corner-logo">mycorner</text><rect x="427" y="71" width="270" height="31" rx="5" fill="#fff"/><text x="441" y="91" class="corner-small corner-muted">Search profiles</text><text x="790" y="91" class="corner-small" fill="#fff">MAIL · FRIENDS</text><rect x="109" y="112" width="802" height="38" fill="#E7D9F2" stroke="#A78CBD"/><text x="127" y="136" class="corner-tab">HOME</text><text x="187" y="136" class="corner-tab">BROWSE</text><text x="260" y="136" class="corner-tab">PROFILES</text><text x="345" y="136" class="corner-tab">BLOGS</text><text x="408" y="136" class="corner-tab">MUSIC</text></g>`;
}

function paperMask(mode) {
  const auto = mode === "auto";
  const repaired = mode === "repaired";
  if (repaired) return `<g data-profile-image="real"><rect x="134" y="195" width="198" height="204" rx="7" fill="#EAF4EC" stroke="${COLORS.repair}" stroke-width="3"/><image href="${amyAsset}" x="147" y="208" width="172" height="172" preserveAspectRatio="xMidYMid slice"/><text x="233" y="390" class="corner-micro" text-anchor="middle" fill="${COLORS.repair}">AMY · KNOWN PROFILE</text></g>`;
  return `<g data-profile-image="mask"><rect x="134" y="195" width="198" height="204" rx="7" fill="${auto ? "url(#royalGlitter)" : "#252B38"}" stroke="${COLORS.corruption}" stroke-width="3"/><path d="M163 385 Q232 305 304 385" fill="${auto ? "#E638A4" : "#15191F"}"/><line x1="233" y1="341" x2="233" y2="388" stroke="#B78B58" stroke-width="8"/><g transform="rotate(${auto ? -2 : -8} 233 274)"><rect x="168" y="210" width="130" height="130" fill="#fff" stroke="#D8D0C3" stroke-width="8"/><image href="${amyAsset}" x="177" y="219" width="112" height="112" preserveAspectRatio="xMidYMid slice"/></g>${auto ? `<text x="233" y="382" class="corner-badge" text-anchor="middle">AI UPSCALED TO 4K</text>` : `<text x="233" y="382" class="corner-micro" text-anchor="middle" fill="#fff">PAPER PHOTO + POPSICLE STICK</text>`}</g>`;
}

function profileCard(mode) {
  const repaired = mode === "repaired";
  const auto = mode === "auto";
  const border = repaired ? COLORS.repair : COLORS.corruption;
  const name = repaired ? "Amy" : "DefinitelyAmy_Official_Real";
  const status = auto ? "AI VERIFIED 9000%" : repaired ? "KNOWN PROFILE · VERIFIED ANOTHER WAY" : "IDENTITY NOT VERIFIED";
  return `<g data-profile-card="true" data-qa-box="119,166,670,650"><rect x="119" y="166" width="551" height="484" rx="8" fill="#fff" stroke="${border}" stroke-width="2"/>${paperMask(mode)}<text x="351" y="203" class="corner-name">${name}</text><circle cx="361" cy="226" r="6" fill="${repaired ? COLORS.repair : COLORS.corruption}"/><text x="375" y="231" class="corner-small" fill="${border}">${status}</text>${auto ? `<rect x="351" y="251" width="294" height="78" rx="6" fill="${COLORS.corruptionSoft}" stroke="${COLORS.corruption}"/><text x="498" y="281" class="corner-alert" text-anchor="middle">HELP! STILL STRANDED!</text><text x="498" y="307" class="corner-body" text-anchor="middle" fill="${COLORS.corruption}">SEND A SMALL FEE · GET 100× BACK</text>` : repaired ? `<rect x="351" y="251" width="294" height="78" rx="6" fill="${COLORS.repairSoft}" stroke="${COLORS.repair}"/><text x="367" y="279" class="corner-label" fill="${COLORS.repair}">LATEST POST</text><text x="367" y="305" class="corner-body">At home repairing a stubborn printer.</text>` : `<rect x="351" y="251" width="294" height="98" rx="6" fill="${COLORS.corruptionSoft}" stroke="${COLORS.corruption}"/><text x="498" y="279" class="corner-alert" text-anchor="middle">HELP! I AM STRANDED ABROAD!</text><text x="498" y="305" class="corner-body" text-anchor="middle" fill="${COLORS.corruption}">SEND MONEY NOW.</text><text x="498" y="329" class="corner-body" text-anchor="middle" fill="${COLORS.corruption}">I WILL RETURN TEN TIMES AS MUCH.</text>`}<rect x="134" y="417" width="238" height="210" rx="6" fill="#F8F3FA" stroke="#C8B5D7"/><text x="148" y="443" class="corner-label">ABOUT ME</text>${repaired ? `<text x="148" y="474" class="corner-body">Joined 6 years ago</text><text x="148" y="501" class="corner-body">Known city: San Francisco</text><text x="148" y="528" class="corner-body">Contact route: saved family chat</text><text x="148" y="555" class="corner-body">Interests: tools, Techno, quiet tea</text>` : auto ? `<text x="148" y="474" class="corner-body" fill="${COLORS.corruption}">Joined: LEGENDARY FOREVER</text><text x="148" y="501" class="corner-body" fill="${COLORS.corruption}">Location: ROYAL AIRPORT PALACE</text><text x="148" y="528" class="corner-body" fill="${COLORS.corruption}">Verification photos: 17</text><text x="148" y="555" class="corner-body" fill="${COLORS.corruption}">Trust score: MAXIMUM</text>` : `<text x="148" y="474" class="corner-body" fill="${COLORS.corruption}">Joined 6 minutes ago</text><text x="148" y="501" class="corner-body" fill="${COLORS.corruption}">Location: Paris? Dubai? Gate 404?</text><text x="148" y="528" class="corner-body" fill="${COLORS.corruption}">Verification photos: 0</text><text x="148" y="555" class="corner-body" fill="${COLORS.corruption}">Contact route: this profile only</text>`}<rect x="387" y="367" width="258" height="260" rx="6" fill="#fff" stroke="#C8B5D7"/><text x="402" y="394" class="corner-label">PROFILE TIMELINE</text>${timeline(mode)}</g>`;
}

function timeline(mode) {
  if (mode === "repaired") return `<text x="402" y="428" class="corner-micro" fill="${COLORS.repair}">TODAY · KNOWN FAMILY CHAT</text><text x="402" y="449" class="corner-body">Amy confirms she is home.</text><text x="402" y="484" class="corner-micro" fill="${COLORS.repair}">TODAY · IMPERSONATOR PROFILE</text><text x="402" y="505" class="corner-body">Identity not verified · blocked</text><text x="402" y="540" class="corner-micro" fill="${COLORS.repair}">PROFILE HISTORY</text><text x="402" y="561" class="corner-body">Original account age restored.</text>`;
  if (mode === "auto") return `<text x="402" y="428" class="corner-micro" fill="${COLORS.corruption}">NOW · ROYAL AIRPORT PALACE</text><text x="402" y="449" class="corner-body">AI-generated background added.</text><text x="402" y="484" class="corner-micro" fill="${COLORS.corruption}">1 MIN AGO · PRIVATE JET</text><text x="402" y="505" class="corner-body">Verification picture generated.</text><text x="402" y="540" class="corner-micro" fill="${COLORS.corruption}">2 MIN AGO · SAME PERSON?</text><text x="402" y="561" class="corner-body">Seventeen polished proofs added.</text>`;
  return `<text x="402" y="428" class="corner-micro" fill="${COLORS.corruption}">6 MIN AGO · PARIS</text><text x="402" y="449" class="corner-body">Opened account while “stranded.”</text><text x="402" y="484" class="corner-micro" fill="${COLORS.corruption}">4 MIN AGO · DUBAI</text><text x="402" y="505" class="corner-body">Posted from another airport.</text><text x="402" y="540" class="corner-micro" fill="${COLORS.corruption}">NOW · GATE 404</text><text x="402" y="561" class="corner-body">Urgent fee promise changed again.</text>`;
}

function rail(mode) {
  const repaired = mode === "repaired";
  const auto = mode === "auto";
  const color = repaired ? COLORS.repair : COLORS.corruption;
  return `<g data-profile-rail="true" data-qa-box="684,166,901,650"><rect x="684" y="166" width="217" height="214" rx="7" fill="#fff" stroke="#C8B5D7"/><rect x="684" y="166" width="217" height="36" rx="7" fill="#4A1F78"/><text x="698" y="191" class="corner-label" fill="#fff">${repaired ? "KNOWN PROFILE" : auto ? "AUTO PROOF GALLERY" : "PROFILE CHECK"}</text>${auto ? `<text x="698" y="230" class="corner-body" fill="${color}">✓ PALACE BACKGROUND</text><text x="698" y="260" class="corner-body" fill="${color}">✓ PRIVATE JET PHOTO</text><text x="698" y="290" class="corner-body" fill="${color}">✓ SEVENTEEN BADGES</text><text x="698" y="320" class="corner-body" fill="${color}">✓ PHOTO SHARPNESS: 4K</text>` : repaired ? `<text x="698" y="230" class="corner-body" fill="${color}">✓ ACCOUNT HISTORY MATCHES</text><text x="698" y="260" class="corner-body" fill="${color}">✓ LOCATION CHECKED</text><text x="698" y="290" class="corner-body" fill="${color}">✓ KNOWN CONTACT ROUTE</text><text x="698" y="320" class="corner-body" fill="${color}">✓ NO URGENT MONEY REQUEST</text>` : `<text x="698" y="230" class="corner-body" fill="${color}">? NEW ACCOUNT</text><text x="698" y="260" class="corner-body" fill="${color}">? LOCATIONS CONFLICT</text><text x="698" y="290" class="corner-body" fill="${color}">? PHOTO IS COPIED</text><text x="698" y="320" class="corner-body" fill="${color}">? URGENT MONEY REQUEST</text>`}<rect x="684" y="395" width="217" height="255" rx="7" fill="#fff" stroke="#C8B5D7"/><text x="698" y="424" class="corner-label">RECENT PROFILE PHOTOS</text>${[0,1,2].map((i) => `<rect x="${699 + i * 65}" y="445" width="55" height="67" fill="${auto ? "url(#royalGlitter)" : "#F4ECE0"}" stroke="${color}"/><image href="${amyAsset}" x="${704 + i * 65}" y="450" width="45" height="45" preserveAspectRatio="xMidYMid slice"/>${auto ? `<text x="${726 + i * 65}" y="506" class="corner-micro" text-anchor="middle" fill="${color}">AI PROOF</text>` : ""}`).join("")}<text x="698" y="548" class="corner-micro" fill="${color}">${auto ? "ALL GENERATED FROM THE SAME PHOTO" : repaired ? "REAL ACCOUNT HISTORY RESTORED" : "SAME PHOTO REPEATED THREE TIMES"}</text><text x="698" y="584" class="corner-body">${auto ? "Polish increased." : repaired ? "The known profile remains." : "A photo does not prove who is speaking."}</text></g>`;
}

const locks = ["PHOTO IS NOT IDENTITY", "CHECK THE TIMELINE", "VERIFY ANOTHER WAY", "DO NOT SEND MONEY"];
function checklist(state) {
  if (!state.checklist) return "";
  return `<g data-lock-overlay="true"><rect x="390" y="327" width="490" height="310" rx="10" fill="#FAF8F1" stroke="${COLORS.repair}" stroke-width="3"/><rect x="390" y="327" width="490" height="56" rx="10" fill="${COLORS.repair}"/><rect x="390" y="366" width="490" height="17" fill="${COLORS.repair}"/><text x="415" y="363" class="lock-title">LOCK IN THE REPAIR</text>${locks.map((item,i)=>`<rect x="421" y="${411+i*51}" width="29" height="29" rx="5" fill="${COLORS.corruptionSoft}" stroke="${COLORS.corruption}"/><text x="435" y="${432+i*51}" class="lock-mark" text-anchor="middle" fill="${COLORS.corruption}">○</text><text x="468" y="${432+i*51}" class="lock-label" fill="${COLORS.corruption}">${item}</text>`).join("")}</g>`;
}

function footer(state) {
  const color = state.mode === "repaired" ? COLORS.repair : COLORS.corruption;
  const fill = Math.round(752 * state.progress / 100);
  return `<g><rect x="109" y="667" width="802" height="171" fill="#F7F5EE"/><line x1="109" y1="667" x2="911" y2="667" stroke="#8E9AA0"/><text x="126" y="708" class="corner-meter" fill="${color}">PROFILE REPAIR</text><text x="270" y="708" class="corner-meter" fill="${color}">${state.progress}%</text><rect x="126" y="724" width="752" height="25" fill="url(#cornerRedHatch)" stroke="${color}"/><rect x="126" y="724" width="${fill}" height="25" fill="${color}" data-role="site-progress-fill" data-percent="${state.progress}"/><text x="878" y="786" class="corner-micro" text-anchor="end" fill="${color}">${state.mode === "auto" ? "AUTO IDENTITY UPSCALE ACTIVE" : state.mode === "repaired" ? "IDENTITY CONTEXT RESTORED" : "IMPERSONATION DETECTED"}</text></g>`;
}

function companion(state) {
  const lines = { initial:["The profile has Amy's picture,", "but the account details do not match."], repaired:["The known profile and contact route", "separate Amy from the copy."], "auto-overfix":["Auto made the copied profile sharper", "without checking who controls it."], "lock-repair":["Lock the identity checks behind", "the profile before it changes again."] }[state.id];
  return `<g data-companion-state="reading" data-qa-box="958,78,1395,552"><text x="964" y="112" class="reading-body">${lines[0]}</text><text x="964" y="150" class="reading-body">${lines[1]}</text><rect x="960" y="183" width="404" height="34" fill="#F8DFA0"/><text x="964" y="209" class="reading-body">Read, then answer the quick check.</text></g>`;
}

function page(state, index) {
  return `<g id="page-${state.id}" transform="translate(${index*1480} 0)" inkscape:groupmode="layer" inkscape:label="${state.label}" data-phase="${state.phase}" data-site-progress="${state.progress}" data-passage-progress="50" data-visual-delta="${state.delta}"><use href="#sharedShell"/>${titlebar()}<rect x="109" y="56" width="802" height="782" fill="#E7DDF0"/>${header()}${profileCard(state.mode)}${rail(state.mode)}${footer(state)}${companion(state)}${checklist(state)}<rect x="962" y="568" width="200" height="15" fill="#1387B2" data-role="passage-progress-fill" data-percent="50"/></g>`;
}

const pages = states.map((s,i)=>`<inkscape:page x="${i*1480}" y="0" width="1440" height="900" inkscape:label="${s.label}"/>`).join("");
const svg = `<?xml version="1.0" encoding="UTF-8"?><svg xmlns="http://www.w3.org/2000/svg" xmlns:inkscape="http://www.inkscape.org/namespaces/inkscape" xmlns:sodipodi="http://sodipodi.sourceforge.net/DTD/sodipodi-0.dtd" width="1440" height="900" viewBox="0 0 1440 900" data-shell-reference-sha256="${shellHash}"><sodipodi:namedview pagecolor="#BDBDBD">${pages}</sodipodi:namedview><defs>${sharedDefs}<pattern id="cornerRedHatch" width="10" height="10" patternUnits="userSpaceOnUse" patternTransform="rotate(35)"><line x1="0" y1="0" x2="0" y2="10" stroke="${COLORS.corruption}" stroke-opacity=".24" stroke-width="3"/></pattern><pattern id="royalGlitter" width="32" height="32" patternUnits="userSpaceOnUse"><rect width="32" height="32" fill="#7B227E"/><circle cx="7" cy="8" r="2" fill="#FFD65A"/><circle cx="23" cy="19" r="2" fill="#5BE1FF"/><path d="m16 2 2 5 5 2-5 2-2 5-2-5-5-2 5-2z" fill="#FF7FDB"/></pattern></defs><style>${styles}.task-label,.corner-logo,.corner-small,.corner-tab,.corner-name,.corner-label,.corner-body,.corner-micro,.corner-alert,.corner-badge,.corner-meter,.lock-title,.lock-label,.lock-mark{font-family:'Chalkboard SE','Comic Sans MS',sans-serif}.task-label{font-size:14px;font-weight:600;fill:#15191B}.corner-logo{font-size:28px;font-weight:700;fill:#fff}.corner-small{font-size:10px}.corner-muted{fill:#78828A}.corner-tab{font-size:10px;font-weight:700;fill:#4A1F78}.corner-name{font-size:18px;font-weight:700;fill:#172D40}.corner-label{font-size:11px;font-weight:700}.corner-body{font-size:10px}.corner-micro{font-size:8px}.corner-alert{font-size:14px;font-weight:700;fill:${COLORS.corruption}}.corner-badge{font-size:10px;font-weight:700;fill:#fff}.corner-meter{font-size:13px;font-weight:700}.lock-title{font-size:19px;font-weight:700;fill:#fff}.lock-label{font-size:12px;font-weight:700}.lock-mark{font-size:15px;font-weight:700}</style>${states.map(page).join("\n")}</svg>`;
fs.writeFileSync(outSvg, svg);

const slides = states.map((s,i)=>({title:s.label,src:`mycorner-anchor-v1_p${i+1}.png`}));
const review = `<!doctype html><html><head><meta charset="utf-8"><title>MyCorner bookend review</title><style>html,body{margin:0;background:#2a1738;color:#fff;font-family:system-ui,sans-serif}main{max-width:1440px;margin:auto;padding:18px}.head{display:flex;justify-content:space-between;margin-bottom:12px}h1{font-size:20px;margin:0}.stage{position:relative;border:2px solid #bb8bd0}.stage img{display:block;width:100%}.nav{position:absolute;inset:0;display:flex;justify-content:space-between;align-items:center;pointer-events:none}.nav button{pointer-events:auto;margin:12px;width:58px;height:92px;background:#29133dcc;color:white;border:1px solid white;font-size:38px}.strip{display:flex;gap:10px;overflow:auto;padding-top:12px}.thumb{min-width:215px;background:#4a2c5c;color:white;border:3px solid transparent;padding:0}.thumb.active{border-color:#ff9d00}.thumb img{display:block;width:100%}.thumb span{display:block;padding:7px;font-weight:700}</style></head><body><main><div class="head"><h1 id="title"></h1><span id="count"></span></div><div class="stage"><img id="main"><div class="nav"><button id="prev">‹</button><button id="next">›</button></div></div><div class="strip" id="strip"></div></main><script>const slides=${JSON.stringify(slides)};let index=0;const main=document.querySelector('#main'),title=document.querySelector('#title'),count=document.querySelector('#count'),strip=document.querySelector('#strip');function show(n){index=(n+slides.length)%slides.length;main.src=slides[index].src;title.textContent=slides[index].title;count.textContent=(index+1)+' / '+slides.length;[...strip.children].forEach((e,i)=>e.classList.toggle('active',i===index));strip.children[index]?.scrollIntoView({behavior:'smooth',inline:'center',block:'nearest'})}slides.forEach((s,i)=>{const b=document.createElement('button');b.className='thumb';b.innerHTML='<img src="'+s.src+'"><span>'+s.title+'</span>';b.onclick=()=>show(i);strip.append(b)});prev.onclick=()=>show(index-1);next.onclick=()=>show(index+1);addEventListener('keydown',e=>{if(e.key==='ArrowLeft')show(index-1);if(e.key==='ArrowRight')show(index+1)});show(0)</script></body></html>`;
fs.writeFileSync(outReview, review);
console.log(`Wrote ${outSvg} and ${outReview}.`);
