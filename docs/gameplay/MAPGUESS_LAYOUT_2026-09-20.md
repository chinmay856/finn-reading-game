# MapGuess layout cleanup

At Chinmay's request, the moving-target repair checklist moves from the center of the map to its upper-right. The panel remains within the 330px shared width limit and shrinks from 176px tall to 124px (ready) or 150px (failed, with feedback). Its single instruction wraps onto two lines. The route, home marker, moving library marker, and Snack Palace destination labels remain visible in each attempt state.

AUTO's overflowing route-planner copy wraps within a taller recommendation card, with the alternative route and nearby labels moved below it. AUTO's name stays uppercase. All fifteen frame exports and the endgame AUTO site thumbnail are refreshed with versioned runtime URLs.

The separate all-site layout review covers 128 mapped screen-art states. Other sites remain unchanged pending review of the screenshot findings.
