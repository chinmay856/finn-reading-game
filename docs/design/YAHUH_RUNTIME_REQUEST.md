# Yahuh! Portal runtime packet request

> **Answered:** designer PR #39 froze the requested contract on 2026-07-12.
> The canonical response is
> [`YAHUH_RUNTIME_RESPONSE_2026-07-12.md`](YAHUH_RUNTIME_RESPONSE_2026-07-12.md).
> This request remains as acceptance-history; no Yahuh P0 fixture question is
> currently outstanding.

## Why this packet is needed

The builder has begun the Yahuh! Portal structural campaign from the frozen
three-sort plus three-reconnect contract. The state machine, semantic view,
content gate, and provisional six-module fixture can be implemented without
changing the Reading Engine, but canonical module data, process identities,
slot-5 evidence, and the six-reading manifest are not yet frozen.

Until this packet is returned, Yahuh remains `MIC: OFF`, creates no reading
score, uses only fictional test data, and excludes its slot-5 receipt from the
canonical Case File and final unlock.

## Frozen builder contract

The implementation keeps these production decisions:

- exactly six modules, in this order: News, Weather, Finance, Sports, Mail,
  Sponsored;
- exactly three saved sort units:
  `news_weather_sorted`, `finance_sports_sorted`,
  `mail_sponsored_sorted`;
- saved midpoint `yahuh_single_stream`, followed by explicit acknowledgement;
- exact midpoint proof:

  ```text
  VISIBLE MODULES: 6
  INDEPENDENT CHANNELS: 1
  SAME-MILLISECOND REWRITE: CONFIRMED
  ```

- exactly three reconnect units:
  `news_weather_channels`, `finance_sports_channels`,
  `mail_sponsored_channels`;
- exactly six distinct accepted first-run readings, with no forced retry and
  no percentage;
- saved Act I labels persist through the single-stream overwrite;
- secured label `CATEGORY SWITCHBOARD RESTORED`;
- blocked label `CATEGORY AND SOURCE REQUIRED`;
- evidence title `YAHUH PORTAL / SINGLE STREAM MERGE`;
- category/source/channel relationships remain semantic DOM text;
- only the Reading Companion may ever be speech-scored;
- concept boards remain composition references, never runtime backgrounds.

## P0: canonical six-module fixture

Please return one versioned stable-ID fixture for News, Weather, Finance,
Sports, Mail, and Sponsored. For every module freeze:

1. module ID, title, short summary, and safe accessible summary;
2. category ID and display label;
3. source ID and display label;
4. publication date/time, timezone, and player-facing date label;
5. sponsorship flag and exact sponsorship label;
6. distinct channel ID, display name, and accessible route summary;
7. the sort unit and reconnect unit that own the record;
8. corrupted headline/value before its sort saves;
9. the exact fields replaced by Single Source of Everything;
10. the blocked retry target fields.

Also freeze the one generated origin ID, exact same-millisecond timestamp and
display label, all six replaced-module IDs, and the pre-merge saved-label
snapshot.

The Mail record must be entirely fictional and safe for a public prototype. It
must contain no real sender, recipient, address, message body, child data, or
other private information. The current builder fixture exposes only a local
index count and explicitly contains no private message content.

The current provisional fixture uses original Northwind portal records and is
fully replaceable. Do not promote it by renaming its IDs.

## P0: copy and process resolution

The exact module contract contains six modules, but an older corrupted-body
line also names `shopping`. The builder has provisionally removed that seventh
concept rather than creating a Shopping module. Please either confirm the
six-name body or explicitly define shopping as content inside the Sponsored
module.

Please also resolve whether `PORTAL AUTO-FIX AI` and `AUTO-LAYOUT` are aliases,
parent/child processes, or distinct jobs. Freeze:

- player-facing process name or names;
- stable actor/process IDs;
- upstream service ID;
- midpoint writer fingerprint;
- blocked-write actor and blocked target ID;
- whether this already-authored Chinmay line is canonical or intentionally
  omitted from the copy manifest:

  ```text
  Separate categories were legacy clutter. I am now open to the possibility
  that clutter was doing useful work.
  ```

The builder currently treats both process names as unresolved jobs mapped to
`ai_repair_service`; it does not assert that they are the same process.

## P0: canonical slot-5 registry row

The campaign hub already freezes these descriptions:

```text
What changed:
Portal modules now show category, source, date, and sponsorship labels.

AI service behavior:
The portal merger replaced distinct channels with one generated stream.
```

Please provide the complete canonical row: evidence ID, filename, short label,
asset ID or explicit null, principle binding, writer fingerprint, upstream
service and route-fragment fields, observed order/time, blocked-write ID,
blocked actor, blocked target, and canonical eligibility fields.

The decorative midpoint timestamp cannot double as the durable blocked-write
time. That observed time must come from the actual secured/blocked event.

## P0: six-reading content manifest

The planned roster is still 5A + 5B, while the campaign requires six distinct
first-run readings. Please:

1. approve or replace the builder-proposed A01 ID
   `the-newspaper-that-found-people-on-the-moon-a01`;
2. confirm that A01 remains Deck A position 1;
3. author one A06 record, or explicitly promote a reviewed record such as
   planned B03 `What Counts as News` with a stable migration decision;
4. return six individually reviewed eligible record IDs.

The builder has converted the existing 241-word sampler into one theme-neutral
structured candidate. Its source trail is Project Gutenberg eBook 62779,
frozen at the item update dated 2024-10-18, with factual cross-checking against
the Library of Congress essay `Peoples & Creatures of the Moon`. It remains
fail-closed pending independent adaptation, factual, editorial, grade,
sensitivity, comprehension, accessibility, reading-profile, rights, and real
microphone review.

Current visible gate:

```text
10 PLANNED - 1 STRUCTURED CANDIDATE - 0 SELECTABLE - 6 REQUIRED
MIC: OFF
NO READING SCORE
```

The runtime will not repeat a passage or silently borrow a Deck B record to hide
the one-reading shortfall.

## P1: interaction, responsive, and character decisions

Please confirm or replace these provisional builder defaults:

- initial state ID `yahuh_corrupted`;
- midpoint action label `Keep the saved labels and reconnect source channels`;
- at 1440 CSS pixels, a two-by-three module grid, right switchboard, and bottom
  source log;
- at 1180-1279 CSS pixels, the compact circuit summary stays visible while the
  detailed route/source log becomes one focus-managed site-owned drawer;
- saved-label comparison lives inside that same drawer rather than creating a
  second overlay;
- character mappings use `amy_evidence`, `chinmay_confident`, and
  `chinmay_fluster_1` unless a focused runtime pack supersedes them;
- exact Techno runtime asset for the visible label
  `DOG TOY - NOT BREAKING NEWS` and accessible meaning that Techno's ball is
  correctly classified in Sports.

Reduced motion will switch directly to the merged view and trace summary with
the same semantic records and no module-collision animation.

## P1: hub continuation

Please freeze the Incoming Cases shown after Yahuh secures and state whether
its local evidence payoff triggers a separate count-based campaign beat. Supply
stable state/route IDs and exact copy for any new global beat.

## Builder acceptance test for the returned packet

The packet is implementation-ready when the builder can prove:

- six modules and no hidden seventh Shopping module;
- each of six reading units changes exactly its authored module pair;
- all six midpoint modules share one origin and timestamp while saved labels
  remain inspectable;
- final switchboard routes use six distinct channel IDs;
- Mail contains no private or real-person data;
- the blocked write names one actor and one target;
- the evidence row satisfies the fixed canonical registry predicate;
- six distinct passages are explicitly first-run eligible;
- 1440 and 1180 compositions, focus behavior, and reduced motion are explicit;
- no runtime raster board, real portal identity, or site audio is required.
