# Runtime fixture and endgame data backlog

This is the builder-to-designer backlog for exact runtime data that is still
missing after the production package through design tip `1f843bc`. The campaign
state sequences, site tokens, broad layouts, copy packs, original marks, and
shared character direction are already frozen. Do not create another ten-site
board to answer these requests.

The useful deliverable is one focused Markdown, JSON, or JavaScript data file
containing original fictional records and stable IDs. Runtime fixtures must not
copy real posts, profiles, reviews, listings, maps, tracks, creator transcripts,
or product chrome.

## Immediate implementation packets

### ThreadIt forum fixture

Provide the original question and four to six replies with stable IDs, authors,
timestamps, vote counts, parent relationships, citation text, origin IDs, the
exact duplicate group, and concise accessible relationship summaries. Identify
the displaced question, the generated clone group, and the blocked post target.

The semantic seven-unit campaign already runs with explicitly provisional
records, so this fixture can replace data without changing state or layout.

### FacePlace feed fixture

Provide:

- one fictional profile identity;
- relationship clusters with stable IDs, labels, member IDs, and accessible
  summaries;
- five to seven feed cards with stable IDs, author, timestamp, body, reactions,
  `chronological` or `recommended` type, duplicate group, and source/origin;
- the recommendation reason for each recommended card;
- `People You May Sort Of Know` entries;
- the exact card targeted by the final boost attempt.

Also freeze the empty-state tracker text before the authored `12% -> 114% ->
AVOCADO%` sequence; the relationship among `FEED AUTO-FIX AI`, `VIBESHIFT AI`,
and the amplification job; FacePlace-specific Chinmay midpoint copy or an
explicit omission; the writer fingerprint; evidence filename/short label; and
the blocked actor/card IDs.

Confirm whether the production mark, semantic DOM/CSS feed, and shared
character crops are sufficient. No FacePlace-specific avatar, tracker,
secured-badge, or evidence-icon pack currently exists, and none is required if
DOM/CSS treatment is approved.

### Shared evidence-route registry

The final breach cannot unlock from a visual counter alone. Publish one
wrapper-owned registry row for every site:

```text
siteId
slot
evidenceId                 # logical durable record ID
assetId                    # nullable presentation-asset ID
filename
title
shortLabel
principle
whatChanged
aiBehavior
localWriterFingerprint
upstreamServiceId
writeObservedAt            # durable event time, never fixture display time
traceOrder                 # deterministic fallback when times are intentionally fictional
routeFragment.id
routeFragment.from
routeFragment.fromLabel
routeFragment.to
routeFragment.toLabel
routeFragment.relationshipLabel
routeFragment.accessibleSummary
blockedWriteId
blockedActorId
blockedTargetId
```

WikiWhy already freezes `wikiwhy.evidence.route-fragment-01` and
`generated_summary -> site_publish_gate`. ThreadIt freezes
`threadit.evidence.synthetic-consensus-overflow-01`, but still needs its global
route fragment and `consensus_auto_fix -> ai_repair_service` mapping. Sites
three through ten need stable evidence IDs, filenames, local-writer mappings,
and route fragments. Keep Case File slots fixed by site number even when sites
are completed in a flexible order. `evidenceId` is the logical record ID;
`assetId` is nullable. WikiWhy may keep its current compatibility alias where
the logical evidence and route-fragment asset share an ID.

Every real row must ultimately resolve to upstream service
`ai_repair_service`, even when its local job name and blocked actor differ. If a
fictional timestamp is not appropriate, use deterministic `traceOrder` and
derive observed time from the durable blocked-write event or `securedAt`; never
reuse a decorative fixture timestamp.

The positive final-unlock predicate is fixed: ten distinct canonical site IDs,
each durable state with `secured === true`, and each persisted `evidenceId`
matching the fixed registry row and slot. Static rows, previews, diagnostics,
tab-only state, or displayed counts never satisfy it.

## Remaining site data packets

### MyCorner

Freeze one canonical profile fixture: owner identity; About, posts, comments,
friends, groups, theme, original fictional music metadata, counter, privacy,
and source-view values; the six saved choices used in the midpoint proof;
`chinmay_demo_profile` comparison values; and the blocked target profile.

Resolve whether `TOM-ISH THEME GENERATOR`, `PROFILE AUTO-FIX AI`, `VIBESHIFT
AI`, and `AUTO-PERSONA` are aliases, parent/child processes, or distinct jobs.
Freeze the player-facing name, writer fingerprint, and blocked-write actor.
Confirm that DOM/CSS theme swatches and initials are sufficient, or provide a
small original avatar/theme-fragment pack rather than a whole board.

### Yahuh! Portal

Freeze six modules: titles/summaries, category/channel IDs and display names,
source IDs, dates/times, sponsorship flags, one safe fictional Mail record,
distinct route summaries, the single generated origin/timestamp, and the
blocked merge targets. Add the already-authored Chinmay midpoint line to the
copy pack or explicitly omit it. Resolve whether `PORTAL AUTO-FIX AI` and
`AUTO-LAYOUT` are the same process or parent/child processes.

### ViewTube

Freeze one entirely original silent-video fixture: recording title, creator,
date, duration, frame IDs/descriptions/timestamps, decorative site transcript,
source/context records, recommendations/comments, the final-eight-second
segment, one origin ID/hash, ten duplicate IDs, three evidence-track records,
accessible summaries, and the blocked clone target.

Confirm whether abstract CSS/inline-SVG frames are canonical or provide a
small original frame/thumbnail pack. The concept board cannot be cropped into
playable media, and version one will contain no autoplay audio.

### Search-ish

Freeze the search query and terms, the generated answer, and four result cards
with exact title/snippet, domain, date, author/publisher, ad status, query match,
shared corrupted-cache ID, restored origin/branch ID, accessible origin summary,
and the blocked placement target. Freeze the explicit midpoint acknowledgement
label plus primary, independent, and generated branch records.
Confirm that the 1180-pixel branch inspector is a site-owned drawer with focus
return to its opener.

### Amaze-On

Freeze the fictional product/listing IDs, titles/descriptions, recommendation
carousel, and four evidence parcels with their claims and source types. The
review parcel needs exact author, date, and context because unit two restores all
three. Freeze the five parcel destinations and exact midpoint action/label, then
the canonical receipt/order IDs and fields for item, seller, recommendation
source, price, return state, replacement count, consent, and blocked purchase
target. Keep all retailers, products, reviews, and people original.

### Spotty-Fi

Freeze five track records: IDs, titles, fictional creators, durations, credits,
order, geometric cover IDs, and the `repeatedTrackId`. Freeze the fake-history
entry/origin that produces the repeated queue, the real-history records and
start point restored later, and the optional suggestion IDs/labels returned in
the finale. State whether the restored manual order is authored or genuinely
player-arranged. Freeze a complete date/time/timezone for the account-created
versus predicted-history proof. The canonical blocked-write label remains
`NOT REQUESTED`; superseded board wording is not runtime copy.

Confirm that version one is entirely silent and that geometric covers may be
DOM/CSS/inline SVG. If actual sound is required later, it needs a separate
rights-cleared pack and an exact rule that mutes it during microphone capture.

### MapGuess

Freeze one entirely fictional incident: destination, original and moved
coordinates, three landmarks and their coordinates, scale, map date, source
identity, sponsored-stop text, and midpoint before/after proof. Include the
Act-I tile/feature IDs, restored place names, terrain/water records, road and
route-segment records, and accessible geometry summaries. Supply four
goal-specific route variants (`fastest`, `safest`, `scenic`, `accessible`),
tradeoff text, directions, and honest ETA treatment.

Confirm that the goal is selected after midpoint and before the third anchor
reading, may change until that reading begins, and appears in the final receipt.
Confirm that responsive DOM/inline SVG is the intended map source; no external
tile service or real user location will be used.

## First-run content manifest decisions

Structural diagnostics stay `playable: false`, `MIC: OFF`, and `NO READING
SCORE` until every required first-run record passes provenance, rights,
factual/adaptation, grade, sensitivity, comprehension, reading-profile, and
real-microphone review. Candidate records are never silently scored or repeated.

| Site | Required distinct first-run readings | Structured records now | Planned Deck A | Decision requested |
| --- | ---: | ---: | ---: | --- |
| ThreadIt | 7 | 1 candidate | 5 | Author, structure, and review two A06/A07 additions, or name two explicit reviewed B overflows. |
| FacePlace | 6 | 0 | 5 | Convert the sampler to a candidate; plan `Why This Appeared` as A06, or name one reviewed B overflow. |
| MyCorner | 7 | 0 | 5 | Author two additions; builder suggests planned titles `Autoplay Is Not Personality` and `The Template Problem`. |
| Yahuh! Portal | 6 | 0 | 5 | Author one addition; builder suggests planned title `What Counts as News`. |
| ViewTube | 7 | 0 | 5 | Author two additions; builder suggests planned titles `One Scene, Three Cuts` and `The Duplicate Frame`. |
| Search-ish | 7 | 0 | 5 | Author `The Domain Clue` and `When Results Disagree` as A06/A07. |
| Amaze-On | 7 | 0 | 5 | Author `The Free Sample Problem` and `The Seller Behind the Listing` as A06/A07. |
| Spotty-Fi | 8 | 0 | 5 | Author three A additions or explicit reviewed B overflows. |
| MapGuess | 8 | 0 | 5 | Author three A additions or explicit reviewed B overflows. |

Only ThreadIt A01 is currently a structured candidate record; the later-site
A01 texts are Markdown samplers and all other titles are planning rows. Deck
labels do not author or promote content. Each new or moved title must become a
structured record and pass the full content and microphone gate independently.

## `EVIDENCE_11.LIVE` data and implementation gaps

The production sequence is already frozen: ten durable real evidence receipts,
genuine finish, player-opened Case File, live-evidence arrival, safety gate,
then exactly three checkpoint readings for trace, preserve, and revoke.

Freeze the durable Evidence 11 record separately from presentation asset ID
`endgame.evidence11.live`. `LIVE_EVIDENCE_11` is the optional internal state key,
not visible copy. Visible identity is already frozen as filename
`EVIDENCE_11.LIVE`, human label `LIVE EVIDENCE 11`, initial
`SOURCE: RECOVERY DESKTOP`, and an unknown writer. The record contract needs:

```text
evidenceId
assetId
discoveredAt
arrivalStatus             # ARRIVING NOW
initialWriter             # null
verifiedWriter
verifiedRoute
preservationStatus
finalizationStatus        # ATTEMPT BLOCKED / RECORD FINALIZED
blockedAttemptId
```

The following behavior is already canonical implementation truth, not a new
designer decision:

- create slot eleven only at discovery; never show a hidden empty slot first;
- start strong decorative desktop corruption only after safety acceptance;
- save checkpoint-three reading acceptance before the separate persisted
  revocation confirmation, and finalize the checkpoint only after both;
- keep archive replay unable to change the saved ending.

Remaining decisions/data:

1. Decide the honest blocked-storage behavior before containment. Builder
   recommendation: do not start until durable local saving works; preserve an
   always-available exit.
2. Freeze exact resume destinations for `Exit and resume later` and `Save and
   exit`.
3. Define the postgame archive flag used by `Replay final incident` while
   retaining the already-frozen no-mutation guarantee.
4. Supply three reviewed, unseen endgame records, each grades 10–12 and 200–280
   scored words, with stable IDs and one-to-one assignment to trace, preserve,
   and revoke. Each still needs comprehension, profile, provenance, and real-
   microphone review.

The ten-warning convergence must exist as semantic DOM data as well as the
approved containment-route visual. Diagnostic, simulated, and tab-only site
states never count toward the final trigger. The visible hub copy now uses
`EVIDENCE_11.LIVE`; keep `LIVE_EVIDENCE_11` internal-only. The final-breach
brief's visible naming above is authoritative.

## Response format

Prefer one focused response file under `docs/design/` with stable IDs and a
short decision table, then publish its commit reference on the design pull
request. Mark genuinely unresolved fields as unresolved instead of filling
them with board-generated microcopy. The builder will keep clearly labeled
provisional wrapper data wherever the frozen state machine can proceed safely.
