# FacePlace runtime data request

This is the focused implementation-to-design packet for the FacePlace Honest
Zero milestone. Please answer with one compact Markdown, JSON, or JavaScript
data file and publish its commit on the design pull request. Stable IDs and
explicit omissions are more useful than another full-board mockup.

## Synchronization point

- Deployed `main`: `e776471` through PRs #34 and #35; FacePlace CI, Pages, and
  complete public HTTPS diagnostic verification passed.
- Current implementation branch: `agent/mapguess-moving-target-foundation`;
  FacePlace is already merged and deployed.
- Last integrated design tip: `1f843bc`.
- Executable FacePlace shape: three false-tracker units, Honest Zero
  acknowledgement, three honest-recovery units, then a blocked boost and slot-3
  test receipt.
- Content gate: ten planned records, zero selectable records, and six distinct
  first-run readings required.
- Only WikiWhy is currently speech-playable.

The deployed implementation has a semantic, responsive six-card feed;
relationship and source views; ranked/chronological order controls; a Why panel;
the lying tracker; Honest Zero; a permanent secured payoff; and a Recovery Map
receipt. Its corrupted, Honest Zero at 1180 CSS pixels, and secured captures are
published under [`review/`](review/README.md).
The current fictional people, posts, graph, process IDs, evidence fields, and
receipt are deliberately **provisional/test-only**. They are replaceable data,
not design canon. The test receipt is excluded from canonical evidence counts
and **cannot unlock the final incident**.

## P0 response packet

### 1. Canonical feed fixture and six-unit assignments

Supply one original fictional dataset containing:

- one profile ID, display name, handle, short profile text, and accessible
  summary;
- five to seven feed-card records with stable card, author, and origin IDs;
- exact author label, timestamp, body, reaction count, and source for each card;
- `chronological` or `recommended` classification and the exact recommendation
  reason for every recommended card;
- relationship clusters with stable IDs, labels, member IDs, and accessible
  summaries;
- `People You May Sort Of Know` records with stable IDs and relationship reason;
- the duplicate/source group and accessible source-relationship graph;
- the exact card targeted by the final boost attempt; and
- a one-to-one assignment of the fixture changes/proofs to all six campaign
  units: false-tracker 1–3 and honest-recovery 1–3.

The runtime must be able to explain what each accepted reading changed without
inventing record-level facts from a concept board.

### 2. Initial tracker value

Freeze the value and label shown before unit one. The three false results after
units one through three are already structurally represented as `12%`, `114%`,
and `AVOCADO%`; the pre-reading empty state is still unresolved. State whether
it is blank, pending, zero, or another exact value, and provide accessible text
that does not imply real progress before a repair occurs.

### 3. Process mapping

Define the relationship among:

- player-facing `FEED AUTO-FIX AI`;
- the concept-board `VIBESHIFT AI AMPLIFICATION JOB`; and
- upstream hostile service `ai_repair_service`.

Mark them as aliases, parent/child processes, or distinct jobs. Freeze the
player-facing label, stable process IDs, local writer fingerprint, and upstream
mapping. Builder recommendation: retain `FEED AUTO-FIX AI` as the local label
and resolve every real evidence route upstream to `ai_repair_service`.

### 4. Canonical evidence and blocked-write route

Provide the canonical slot-3 registry row with:

```text
siteId
slot
evidenceId
assetId                 # nullable
filename
title
shortLabel
principle
whatChanged
aiBehavior
localWriterFingerprint
upstreamServiceId
writeObservedAt         # or deterministic traceOrder
routeFragment.id
routeFragment.from
routeFragment.fromLabel
routeFragment.to
routeFragment.toLabel
routeFragment.relationshipLabel
routeFragment.accessibleSummary
blockedWriteId
blockedActorId
blockedTargetId
```

Name the exact boosted-card target and the actor that attempts the blocked
write. The implementation will continue treating its present IDs and filename
as test-only until a persisted receipt matches this canonical row.

### 5. FacePlace Chinmay midpoint

Choose one:

1. Explicitly omit Chinmay from the site-local Honest Zero midpoint; or
2. Supply the exact FacePlace midpoint copy and approved existing Chinmay crop
   ID.

Amy's role is already sufficient to explain Honest Zero. Do not add Chinmay
merely to fill a panel; if included, he remains sincere, long-haired,
increasingly flustered, and never the hostile writer.

### 6. Asset sufficiency

Confirm whether the existing FacePlace production mark, semantic DOM/CSS feed,
initials/geometric profile treatment, CSS tracker, shared character crops,
secured treatment, and text evidence receipt are sufficient. If not, request a
small named original asset pack with exact runtime use. No whole-page raster
board is needed for the executable feed.

### 7. Six-record first-run content manifest

Freeze six distinct reviewed first-run records and their Deck A/B placement.
Please decide whether to:

1. Promote planned `Why This Appeared` to `why-this-appeared-a06`; or
2. Keep five Deck A slots and designate one specific reviewed Deck B overflow.

Also freeze the intended ID and Deck A/B position for `A Second Reading`.
Its current structured record is candidate-only and must still pass provenance,
adaptation fidelity, grade, sensitivity, three-choice comprehension, reading
profile, and real-microphone review. A deck decision does not promote it.

### 8. Post-FacePlace hub beat

Freeze the exact three Incoming Cases after FacePlace becomes the third
canonically secured site. Also state whether the three-evidence global dialogue
fires only after Finn returns to the Recovery Map, after the local evidence
payoff but before navigation, or at another exact boundary. Builder
recommendation: finish the local blocked-write/evidence payoff, then fire the
global beat on hub return.

### 9. Counter copy drift

Choose the canonical Recovery Map label:

- `Corrupted signals logged`; or
- an exact-number version such as `3 corrupted signals logged`.

If the number is visible, specify whether it counts only canonical persisted
evidence. Provisional, diagnostic, tab-only, and test receipts must never inflate
that number.

### 10. 1180-pixel responsive behavior

Confirm that at 1180 CSS pixels the left profile/relationship rail becomes a
keyboard-addressable drawer owned by the Recovery Browser. It must return focus
to its opener, close with Escape, avoid page overflow, and never overlap the
independent Reading Companion. If a different panel collapses instead, name it
and provide the exact priority order.

## Acceptance criteria for the response

- All people, posts, and sources are original fictional material with stable
  IDs and accessible summaries.
- Each of the six campaign units maps to an objective fixture change or proof.
- The local process, upstream AI service, evidence route, blocked actor, and
  blocked target agree.
- The first-run manifest names six distinct records without a silent repeat.
- The hub beat and exact post-site Incoming Cases are unambiguous.
- Any genuinely unresolved field is labeled unresolved rather than filled with
  decorative board copy.
- No response promotes candidate prose or turns the provisional slot-3 receipt
  into canonical evidence merely by renaming it.

When this packet is answered, update
[`DESIGN_REQUESTS.md`](DESIGN_REQUESTS.md) with the response commit so builders
have an objective synchronization point independent of chat history.
