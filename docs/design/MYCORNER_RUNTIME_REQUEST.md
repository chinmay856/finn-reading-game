# MyCorner runtime packet request

> **Answered:** designer PR #38 froze the requested contract on 2026-07-12.
> The canonical response is
> [`MYCORNER_RUNTIME_RESPONSE_2026-07-12.md`](MYCORNER_RUNTIME_RESPONSE_2026-07-12.md).
> This request remains as acceptance-history; no P0 MyCorner fixture question is
> currently outstanding.

## Why this packet is needed

The builder is preparing the MyCorner structural campaign from the frozen
4-plus-3 production contract. The runtime can preserve all architectural and
accessibility boundaries with DOM/CSS and the existing original mark, but the
current design documents do not yet freeze the canonical profile fixture,
process relationships, evidence row, or seven-reading manifest.

Until this packet is returned, the implementation will use clearly fictional,
test-only profile data. It will remain `MIC: OFF`, create no reading score, and
exclude its slot-4 receipt from the canonical Case File and final unlock.

## Frozen builder contract

The implementation will keep these decisions exactly as written unless design
publishes a superseding canonical contract:

- four restore units: `owner_about`, `theme_friends`, `media_counter`, and
  `privacy_source`;
- saved midpoint `mycorner_template_reveal`, followed by an explicit user
  acknowledgement;
- three owner-lock units: `profile_owner_lock`,
  `presentation_owner_lock`, and `global_apply_blocked`;
- exactly seven distinct accepted first-run readings, with no forced retry and
  no percentage;
- a six-slot scrapbook rail for the four restored module groups and the first
  two owner-lock groups, plus a separate global-permission seal;
- midpoint proof:

  ```text
  SAVED OWNER SNAPSHOT: six distinct module choices
  ACTIVE TEMPLATE: chinmay_demo_profile
  APPLY TO EVERYONE: active
  ```

- saved owner modules remain in the DOM beneath the template comparison;
- `VIEW SAVED PROFILE` changes only the comparison view and never changes
  progress or acknowledges the midpoint;
- secured label `OWNER CONTROLS RESTORED`;
- blocked label `OWNER PERMISSION REQUIRED`;
- evidence title `MYCORNER / GLOBAL PROFILE TEMPLATE`;
- all music remains silent; the provisional build contains no audio asset;
- only the Reading Companion may ever be speech-scored;
- concept boards remain reference material rather than runtime backgrounds.

## P0: canonical profile fixture

Please return one versioned, stable-ID fixture that freezes:

1. the profile owner ID, display name, initials or avatar asset, handle, current
   mood, and privacy label;
2. owner-written About text and source ID;
3. exact posts and comments, including author IDs, parent IDs, timestamps, and
   source IDs;
4. friend and group IDs, labels, membership, order, and accessible summaries;
5. theme ID, name, exact swatches/fragments, and ownership metadata;
6. original fictional music title/artist metadata, with autoplay off and no
   audio asset for version one;
7. visitor-counter value and label;
8. exact privacy-control labels and values;
9. source-view lines and their accessible summary;
10. the exact six saved midpoint choices, in order, with stable IDs, labels,
    values, and owning unit IDs;
11. every `chinmay_demo_profile` comparison value;
12. the blocked target profile ID and each field the AI attempts to overwrite;
13. every corrupted-state placeholder value, including the generated owner,
    About text, post, friend label, music claim, counter, privacy value, source
    state, and theme. The builder fixture now keeps these records in the same
    replaceable data object rather than hard-coding them in the view.

Every person, handle, post, comment, relationship, and profile in the packet
must be explicitly fictional, contain no real-person or child PII, and be safe
to publish in a public prototype.

The current builder fixture uses original fictional text and initials only. It
is deliberately replaceable and must not be promoted by renaming its IDs.

## P0: process and evidence registry

The campaign hub already freezes these exact evidence descriptions:

```text
What changed:
Profiles keep owner-written identity, chosen themes, and visible privacy
controls.

AI service behavior:
The profile generator treated one polished demo account as the template for
everyone.
```

The documents currently name `PROFILE AUTO-FIX AI`, `TOM-ISH THEME GENERATOR`,
`VIBESHIFT AI`, and `AUTO-PERSONA`. Please state whether these are aliases,
parent/child processes, or distinct jobs, then freeze:

- the one player-facing process name used in the blocked-write payoff;
- stable actor/process IDs;
- the upstream service ID;
- the writer fingerprint;
- the blocked-write actor and blocked target ID;
- the complete canonical slot-4 evidence registry row: evidence ID, filename,
  short label, asset ID if any, writer fingerprint, upstream service/route
  fields, and canonical eligibility fields.

The provisional implementation uses a local `AUTO-PERSONA` display actor mapped
to `ai_repair_service`, a clearly provisional filename, and
`canonicalEvidenceId: null`. Canonicalization must happen only through the
fixed evidence registry row.

## P0: seven-reading content manifest

`A Cabin with a Purpose` is already planned at Deck A position A01. Please
approve or replace the builder-proposed stable ID
`a-cabin-with-a-purpose-a01`, confirm A01, then choose one of these explicit
ways to fill the seven-reading
first run while keeping exactly ten planned records total:

1. retain the current 5A + 5B organization and name two individually reviewed
   Deck B records as approved first-run overflow; or
2. explicitly revise the manifest to 7A + 3B, provide the complete ten stable
   IDs and any migration aliases, and place newly reviewed `Autoplay Is Not
   Personality` and `The Template Problem` at A06/A07.

Return seven distinct reviewed records. The runtime will not repeat a passage,
borrow unreviewed Deck B records, or treat a title/deck-label change as content
approval. `A Cabin with a Purpose` remains a candidate until provenance,
adaptation fidelity, factual, grade, sensitivity, comprehension, accessibility,
reading-profile, and real-microphone review all pass.

The current code keeps the 5A + 5B plan and selects Deck A only until that
decision is returned. It contains one structured candidate and zero selectable
records; the shared approved-content gate now also requires an explicit passed
accessibility review. Current visible gate:

```text
10 PLANNED - 1 STRUCTURED CANDIDATE - 0 SELECTABLE - 7 REQUIRED
MIC: OFF
NO READING SCORE
```

## P1: visual and responsive decisions

Please confirm or replace these explicitly provisional builder defaults:

- whether initials plus DOM/CSS theme fragments are sufficient, or provide a
  small original avatar/theme-fragment pack rather than another full board;
- midpoint primary action label: `Keep the saved profile and restore owner
  controls`;
- the active template comparison opens first, with a reversible
  `VIEW SAVED PROFILE` control;
- the right-side owner/source/permission inspector becomes a drawer at
  1180-1279 CSS pixels and opens automatically for acknowledgement and secured
  evidence;
- character mappings for the early claim, source inspection, midpoint, and
  secured beats;
- the exact Techno still. The builder currently pairs the existing ball still
  with a visible `GENERATED PROFILE: BALL / REAL OBJECT: BALL` comparison while
  preserving this accessible meaning:
  `Techno compares the generated BALL profile with the real ball under her
  paw.`

## P1: hub continuation

Please freeze the three Incoming Cases shown after MyCorner secures. Also state
whether the MyCorner local evidence payoff triggers a separate global campaign
beat and, if so, supply its copy and stable route/state ID.

The builder has deliberately removed an invented three-site replacement. Until
this answer arrives, the diagnostic hub filters out secured sites and may show
fewer than three Incoming Cases rather than presenting an unfrozen order as
campaign truth.

## Builder acceptance test for the returned packet

The packet is implementation-ready when the builder can prove all of the
following without inventing data:

- each of seven campaign units maps one-to-one to visible fixture changes;
- the midpoint compares six saved owner choices with one explicit template;
- saved work persists before and after acknowledgement;
- source, privacy, owner, media, and theme values have stable IDs;
- the blocked write names one actor and one target;
- the evidence row is sufficient for the fixed canonical registry predicate;
- seven distinct passages are explicitly eligible for the first run;
- the 1440 and 1180 compositions are unambiguous;
- silent media and reduced-motion behavior are explicit;
- no runtime raster board or real-product identity is required.
