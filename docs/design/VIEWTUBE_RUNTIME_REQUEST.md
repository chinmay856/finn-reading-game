# ViewTube runtime packet request

## Why this packet is needed

The builder is preparing ViewTube from the frozen four-restore plus three-track
campaign contract. The state machine and semantic page can be built around a
replaceable fictional fixture, but ten runtime decisions remain unresolved.
Please answer these questions in one focused Markdown, JSON, or JavaScript data
packet with stable IDs. Another broad concept board is not needed.

Until a canonical packet and eligible passage manifest arrive, ViewTube must
remain `MIC: OFF`, create no reading score, use only fictional test data, and
exclude any slot-6 receipt from the canonical Case File and final unlock.

## Frozen builder contract

The implementation will preserve these production decisions:

- exactly four saved recording-context units:
  `recording_identity`, `distinct_frames`, `transcript_track`, and
  `source_context`;
- saved midpoint `viewtube_autoplay_loop`, followed by one explicit player
  acknowledgement;
- exact midpoint proof:

  ```text
  PLAYBACKS: 10
  DISTINCT MEDIA HASHES: 1
  NEW EVIDENCE: 0
  ```

- exactly three saved evidence-track units:
  `footage_track_verified`, `transcript_track_verified`, and
  `source_track_verified`;
- exactly seven distinct accepted first-run readings, with no silent repeat,
  implicit Deck B overflow, forced retry, or percentage;
- secured label `EVIDENCE TRACKS RESTORED`;
- blocked label `DUPLICATE FRAMES - NO NEW EVIDENCE`;
- evidence title `VIEWTUBE / DUPLICATE MEDIA HASHES`;
- the site transcript is decorative context labeled
  `SITE TRANSCRIPT (NOT READING PASSAGE)` and can never be speech-scored;
- the player, timeline, hashes, transcript, sources, and recommendations remain
  semantic DOM/CSS/inline-SVG records rather than a flattened board;
- version one has no autoplay audio, and any future site audio must remain
  silent during microphone capture.

## The ten unresolved questions

### 1. What is the complete original silent-video fixture?

Freeze one entirely original fixture with a version ID and stable IDs for:

- recording title, creator, recording date/time/timezone, duration, and safe
  accessible recording summary;
- every frame's ID, description, timestamp, and accessible summary;
- the decorative site transcript and its timestamp links;
- source/context records, sponsorship state, recommendations, and comments;
- the final-eight-second segment;
- one origin ID and media hash, ten duplicate playback IDs, and their shared
  origin relationship;
- the three final evidence-track records; and
- the exact clone target used by the blocked write.

All people, channels, comments, media, and claims must be fictional. Do not
copy real creators, transcripts, videos, comments, thumbnails, or platform
chrome. Please include the exact fields that change at midpoint and the saved
pre-loop snapshot that remains inspectable afterward.

### 2. Which fixture proof belongs to each of the seven saved units?

Return a one-to-one assignment for these exact unit IDs:

1. `recording_identity`
2. `distinct_frames`
3. `transcript_track`
4. `source_context`
5. `footage_track_verified`
6. `transcript_track_verified`
7. `source_track_verified`

For each unit, name the record IDs revealed, changed, linked, quarantined, or
verified and the short accessible result. No unit should repair the same field
twice, and diagnostic completion must be provable without creating a Reading
Engine score.

### 3. What is the exact midpoint acknowledgement label?

Freeze the player-facing action that follows saved
`viewtube_autoplay_loop`. The acknowledgement must explicitly preserve the
four restored context records and proceed to separate evidence tracks. It must
not look like a reading result, retry, autoplay control, or percentage action.

### 4. Is `viewtube_corrupted` the canonical initial state ID?

Accept that ID or provide the replacement plus a migration/alias rule. Also
state whether the saved midpoint ID remains `viewtube_autoplay_loop`. The
builder will not silently rename persisted states after publication.

### 5. What is the local process identity and upstream mapping?

Freeze the stable local writer/process ID, player-facing name, writer
fingerprint, and relationship represented by:

```text
VIDEO AUTO-FIX AI -> ai_repair_service
```

State whether `VIDEO AUTO-FIX AI` is the local job itself, an alias, or a child
of another process. Name the midpoint writer, secured blocked-write actor, and
upstream service ID explicitly.

### 6. What is the complete canonical slot-6 evidence row?

Return the full fixed registry row: site ID, slot 6, evidence ID, nullable asset
ID, filename, title, short label, principle, `whatChanged`, `aiBehavior`, local
writer fingerprint, upstream service ID, durable observed time or deterministic
trace order, route-fragment IDs/labels/relationship/accessibility summary,
blocked-write ID, blocked actor ID, and blocked target ID.

The durable observed time must come from the actual secured/blocked event, not
from a decorative video timestamp. Please state the exact predicate fields that
make the row canonical and final-unlock eligible.

### 7. Are semantic abstract frames canonical, or is a small asset pack required?

Confirm that responsive DOM/CSS/inline-SVG abstract frames, timelines, hash
badges, and thumbnails are the canonical version-one source. If not, provide a
small original frame/thumbnail pack with stable asset IDs, dimensions, crop
rules, alt text, and reduced-motion treatment. The concept board cannot be
cropped into playable media, and no recognizable video-platform chrome may be
introduced.

### 8. Are the existing character assets sufficient?

Confirm the existing mappings for `amy_evidence`, the campaign-stage-appropriate
`chinmay_fluster_1` or `chinmay_fluster_2`, `techno_suspicious_file`, and
`techno_clue_point`. If `techno_clue_point` cannot be cleanly used in the player
composition, provide one standalone transparent crop with asset ID, dimensions,
placement bounds, and accessible meaning. Techno remains a visual clue and does
not speak or control progression.

### 9. Should recommendations and comments become the 1180px browser-owned drawer?

Confirm or replace this responsive default:

- at 1440 CSS pixels, the central silent player, recommendation rail, context
  panels, and bottom evidence timeline remain simultaneously visible;
- at 1180-1279 CSS pixels, the recommendation/comment detail becomes one
  site-owned drawer inside the Recovery Browser;
- the opener remains visible, focus moves into the drawer, Escape/close returns
  focus to that opener, and background content becomes inert while open;
- source/context proof and the Reading Companion never move into that drawer.

Please include the reduced-motion behavior and exact drawer label.

### 10. What is the seven-record first-run manifest decision?

The planned roster is five Deck A records plus five Deck B records, but the
campaign requires seven distinct first-run readings. Please:

1. approve or replace `the-sky-becomes-a-streak-of-fire-a01` and confirm its
   Deck A position;
2. author and review explicit A06/A07 records (the builder suggestions are
   `One Scene, Three Cuts` and `The Duplicate Frame`), **or** name two explicit
   reviewed Deck B overflow records with stable migration rules;
3. return seven individually reviewed eligible IDs in order; and
4. state the post-secure replay policy separately from the first-run campaign.

The runtime will not repeat an A record or silently consume B records to hide
the two-reading shortfall.

## Verified A01 source trail

The builder converted the existing sampler into one theme-neutral structured
candidate using only the authoritative item and rights pages:

- H. G. Wells, *The Time Machine*, Chapter IV, `Time Travelling`, Project
  Gutenberg eBook 35: <https://www.gutenberg.org/ebooks/35>
- exact item release date: `2004-10-02`;
- exact item last update: `2026-06-16`;
- item rights label: `Public domain in the USA`;
- official permissions guidance:
  <https://www.gutenberg.org/policy/permission.html>.

The scored text is the 255-word passage beginning `I am afraid I cannot convey`
and ending `flickering in the blue`. It matches the current item text, including
British spelling. Only the enclosing speech quotation marks were omitted for a
standalone display. No prose was modernized or paraphrased.

The candidate remains fail-closed pending independent line/adaptation,
source-context, editorial, grade, sensitivity, comprehension, accessibility,
reading-profile, rights/jurisdiction, and real-microphone review.

Current visible gate:

```text
10 PLANNED - 1 STRUCTURED CANDIDATE - 0 SELECTABLE - 7 REQUIRED
FIRST-RUN SHORTFALL: 2
MIC: OFF
NO READING SCORE
```

## Builder acceptance test for the returned packet

The packet is implementation-ready when the builder can prove:

- one original source clip produces ten playback IDs, one media hash, and zero
  new evidence;
- all seven saved units own distinct fixture changes or proofs;
- the midpoint cannot advance without the explicit acknowledgement;
- footage, decorative transcript, and source/context remain distinct semantic
  tracks;
- the blocked write names one stable actor and one stable target;
- the slot-6 row satisfies the fixed canonical evidence predicate;
- seven distinct passages are explicitly first-run eligible;
- 1440 and 1180 layouts, focus return, and reduced motion are explicit; and
- no runtime board crop, real creator identity, real transcript, platform audio,
  or borrowed brand chrome is required.
